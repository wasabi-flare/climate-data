name: NOAA CDO Daily Collection

on:
  schedule:
    # Run at 00:30 UTC every day — targets previous day's data
    - cron: "30 0 * * *"
  workflow_dispatch:
    # Allow manual trigger with optional date override
    inputs:
      target_date:
        description: "Target date (YYYY-MM-DD), defaults to yesterday"
        required: false

jobs:
  collect:
    runs-on: ubuntu-latest
    outputs:
      target_date: ${{ steps.date.outputs.target }}
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Compute target date
        id: date
        run: |
          TARGET="${{ inputs.target_date || (python -c 'from datetime import date, timedelta; print((date.today()-timedelta(days=1)).isoformat())') }}"
          echo "target=$TARGET" >> $GITHUB_OUTPUT

      - name: Install dependencies
        run: pip install httpx

      - name: Run collection
        env:
          NOAA_CDO_TOKEN: ${{ secrets.NOAA_CDO_TOKEN }}
        run: |
          python collect.py --date "${{ steps.date.outputs.target }}" --output-dir ./output

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: noaa-cdo-${{ steps.date.outputs.target }}
          path: output/
          retention-days: 30

      - name: Pin daily to IPFS
        env:
          PINATA_JWT: ${{ secrets.PINATA_JWT }}
        run: |
          CID=$(curl -sS -X POST https://api.pinata.cloud/pinning/pinFileToIPFS \
            -H "Authorization: Bearer $PINATA_JWT" \
            -F "file=@output/daily_${{ steps.date.outputs.target }}.json" \
            | python -c "import sys,json; print(json.load(sys.stdin)['IpfsHash'])")
          echo "daily_cid=$CID" >> $GITHUB_ENV

      - name: Pin monthly aggregate to IPFS
        env:
          PINATA_JWT: ${{ secrets.PINATA_JWT }}
        run: |
          YYYY=$(echo "${{ steps.date.outputs.target }}" | cut -d- -f1)
          MM=$(echo "${{ steps.date.outputs.target }}" | cut -d- -f2)
          CID=$(curl -sS -X POST https://api.pinata.cloud/pinning/pinFileToIPFS \
            -H "Authorization: Bearer $PINATA_JWT" \
            -F "file=@output/monthly_${YYYY}_${MM}.json" \
            | python -c "import sys,json; print(json.load(sys.stdin)['IpfsHash'])")
          echo "monthly_cid=$CID" >> $GITHUB_ENV

      - name: Update onchain registry
        if: env.daily_cid != ''
        run: |
          echo "Artifact 18 registry update: daily CID ${{ env.daily_cid }}, monthly CID ${{ env.monthly_cid }}"
